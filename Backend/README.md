# WellNest

Install Node.js version `v16.15.0` and npm version `8.5.5`

# Getting started

Command to __install__ all __dependencies__  `npm install`

#  Development server
* Run project

  * `npm start` 

* API document endpoints

    * local api url: `http://localhost:3000/api/(api-end-point)`